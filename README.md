## Examples Assynchruns and Thread

* `demo_thread` Is possible a async function, because you can answer de request and run a second thread to process and delivery the information.
* `demo_multiprocess` Is similar `demo_thread` both are very simples.
* `demo_asyncio` Is a native python mode, but I fill not simple to implemented together Flask. Other Web framework like FastAPI have a native mode to Async functions.